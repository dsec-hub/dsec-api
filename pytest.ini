[pytest]
# Run from the dsec-api/ directory. `pythonpath = .` makes the `app` package
# importable without installing it. Tests use a throwaway SQLite DB and never
# touch Neon or OpenAI (see tests/conftest.py).
pythonpath = .
testpaths = tests
addopts = -q
